<div class="mx-4">
  <h4 class="my-4">一覧表示</h4>
	<table class="table table-light table-striped table-responsive-lg">
    <thead>
      <tr>
        <th scope="col">#</th>
        <th scope="col">食べた日</th>
        <th scope="col">店名</th>
        <th scope="col">メニュー</th>
        <th scope="col">コメント</th>
      </tr>
    </thead>
    <tbody>
      <% @posts.each do |post| %>
      <tr>
        <td><%= post.id %></td>
        <td><%= post.event_date %></td>
        <td><%= post.shop_name %></td>
        <td><%= post.menu %></td>
        <td><%= post.comment %></td>
      </tr>
      <% end %>
    </tbody>
  </table>
</div>